# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UnsplashPhotos' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UnsplashPhotos
  pod 'RxSwift', '~> 5.0'
  pod 'RxCocoa', '~> 5.0'
  pod 'Kingfisher', '~> 5.0'
  pod 'Alamofire', '~> 5.2'
  pod 'RxAlamofire'
  
  target 'UnsplashPhotosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UnsplashPhotosUITests' do
    # Pods for testing
  end

end
